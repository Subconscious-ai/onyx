# Active: Beca prototype, issue 18

User authorization: September 12, execute the agreed prototype through completion. Draft previews remain unmerged pending executive QA.

## Outcome and scope

Email/company → automatic public research → sourced Beca interview → reviewed journey/OKRs → saved business model → proposed experiment or authorized existing-study analysis.

Reuse native Onyx, AWS, GPT Researcher MCP, causl-kb safe-door/model storage and the existing calculator. No enterprise identity migration, new agent runtime or paid experiment launch.

## Ownership

The Create unified consulting agent plan task owns Onyx #18: research startup, public-context persistence, server-side brief scheduling and integrated Beca preview QA. Worktree: `worktrees/onyx-prototype`; branch `codex/18-beca-prototype`.

The burn-panel task owns causl-kb #558 / draft #559 and isolated Onyx branch `codex/558-model-scenarios`: scenario handoff, native calculation, explicit approval, save/reopen. Reserved Onyx paths: `lib/executive/model-handoff.ts`, the associated test, and `ExecutiveWorkspace.openModel`. Keep the existing model-review window.

The Enhance causal KB cards task owns causl-kb #549/#556 and Onyx #17 private-document pilot. Preserve those branches and connectors. Shared API/backend publication requires coordination.

## Checkpoint

- Prototype base combines Beca `cab646357f` and merged executive fixes `96db2cf14f`, merge `aaddf7c350`. Hosting resolution retains the verified rootless directory mount and current runtime image. Reminder retains answer-four guidance plus Beca tool-use instructions.
- Actual API image remains `onyx-burn2:executive-f509b393b7`. No runtime changes or deployment performed yet.
- Research source inspection confirms upstream `deep_research` returns context, original source URLs and a research ID. Source data must be persisted locally; upstream memory alone is insufficient.
- Six new pure research checks pass after the initial missing-module failure. Checks cover public-domain-only queries, unsafe/missing profile, nested failures, source URLs, stale cache and untrusted research context.
- Native Postgres research helper, queue scheduler and worker tasks pass 11 native-image boundary checks. Both task registrations and API/process imports pass in a disposable image. Actual Beca persona 5 has deep_research plus Rehoboam tools assigned. Real queue proof remains outstanding.
- Server completion hook schedules brief preparation; GET readback route is added. The browser regression failed against the old synchronous hook, then passed. Eight focused polling/layout checks and the native frontend typecheck pass. Research source links stay inside the existing collapsed context panel.
- Pinned Bun 1.3.13 restored the isolated frontend dependencies. Global Bun remains unchanged. RTK is unavailable; native shell fallback applies. Optional RTK.md and MACHINE.md are absent.

## Remaining acceptance

1. Prove native scheduling: persona/user access, one job per fresh profile, failed queue/tool receipts, tenant propagation and persisted research after browser closure. Keep tool credentials in native encrypted storage.
2. Finish read-only brief polling and compact research status. Preserve manual retry and old-stack fallback. Prove no competing extraction, stale-chat update or lost correction.
3. Integrate the scenario helper from the model task. Resolve an exact paired model destination and preserve exact origins/nonces.
4. Replay three synthetic businesses twice using real AWS interview/research and native calculation. Verify exact source/model readback, correction, explicit unknowns, refresh, mobile layout and authorized study artifacts. Record failures, executive minutes and ordinary latency separately.
5. Publish a protected preview using a clean Git archive and pinned backend overlay. Keep credentials and QA artifacts outside uploads. Preserve customer data; remove task-created synthetic sessions and servers after proof.

## Exact recovery step

Finish the candidate source commit and draft PR. The image candidate passes native import/worker proof; rebuild after the formatting-only process change, then replace only API/background after private configuration snapshots. Model and connector tasks received rollout coordination. Prove real queue completion and saved source continuity, then combine the scenario handoff and protected frontend preview. Refresh the checkpoint after each material result.
