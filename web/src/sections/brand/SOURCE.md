# Subconscious branding

The local Wordmark, DagMark, and brand.css files preserve the public design-system registry components and styles. Source revisions are recorded in each file. The standalone favicon uses the same causal-mark geometry with explicit colors for browser icon rendering. Native provider icons and the upstream About link remain attribution, not application branding.
